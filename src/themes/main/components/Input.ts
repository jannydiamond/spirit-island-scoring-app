import Colors from 'themes/main/global/Colors'

const Input = {
  height: '40px',
  border: `2px solid ${Colors.gray.default}`,
  borderRadius: '4px',
  hover: {
    borderColor: Colors.gray.dark,
  },
  focus: {
    borderColor: Colors.primary.default,
  }

}

export default Input
