import Colors from 'themes/main/global/Colors'

const FloatingButton = {
  background: Colors.primary.default,
  color: Colors.white,
  size: '48px',
  boxShadow: '1px 1px 8px 0px rgba(0, 0, 0, .4)',
  border: 'none',
  borderRadius: '100%',
}

export default FloatingButton
